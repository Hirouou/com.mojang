# IRON RAIN — SHARED AGENT LOG

Use este arquivo para handoff curto entre contas. Adicione entradas novas no topo.

## Modelo

### YYYY-MM-DD HH:MM — [CONTA/SLOT] — [WORKSTREAM]
- **FEITO:**
- **ARQUIVOS:**
- **TESTE:**
- **PRÓXIMO:**
- **RISCO:**
- **COMMIT:**

---

### 2026-09-12 20:54 — ChatGPT-GPT-5.6-Sol / SLOT A — WORLD WAR
- **FEITO:** atualizada a visão da `iron-rain-v6-1-continuation`, relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md`, `AGENT_LOG.md`, commits recentes e, por tocar território/front, também consumidos obrigatoriamente `MAP_REWORK_20260912.md` + `MAP_REFERENCE_20260912.svg`. Continuada a fundação existente sem renderer/mapa paralelo: `sectorCaptureAllowed()` e `shiftControlLine()` agora são compostos por `captureSectorWithFront()`, que resolve uma captura conectada e devolve no mesmo resultado o hex atualizado e uma deformação local da linha canônica. Ganho ALIADO desloca a frente para leste; ganho EIXO desloca para oeste. Captura desconectada falha fechada e preserva ownership + linha, impedindo bolsões mágicos e duas fontes independentes de verdade. Durante o fechamento a branch avançou com dois commits concorrentes em `theatre-mamute-snapshot.js`; comparação confirmou que ambos são descendentes de `3b16ae1d` e não sobrepõem `strategic-hex-map.js` nem seu teste, portanto foram preservados.
- **ARQUIVOS:** `modules/strategic-hex-map.js`, `tests/strategic-hex-map.test.js`, `docs/AGENT_LOG.md`. Consumidos sem reescrever `modules/theatre-control.js`, `modules/strategic-front-pressure.js` ou `modules/strategic-war-live-v3.js`. Confirmado consumo de `MAP_REWORK_20260912.md` + `MAP_REFERENCE_20260912.svg` neste ciclo.
- **TESTE:** adicionada regressão oficial para captura conectada alterando ownership e deslocando a mesma linha canônica, além de captura desconectada preservando ambos. Um harness focal em Node com os contratos reais relevantes passou 2/2 após `node --check`: `connected capture bends ally front east` e `disconnected capture fails closed`. O `npm test` completo não pôde ser executado porque o checkout local continua sem DNS para `github.com`. Após os commits de código/teste, a branch foi relida; `3b16ae1d` permaneceu ancestral do HEAD concorrente `3a9a0cef`, sem arquivos sobrepostos.
- **PRÓXIMO:** substituir o caminho real que hoje altera ownership de setor por `captureSectorWithFront()` e persistir o `line` devolvido como a linha estratégica ativa; em seguida fazer `strategic-war-live-v3.js` desenhar essa linha/`strategicFrontPath()` em vez de depender apenas do `lineSnapshot()` default. A integração deve ser atômica: não manter captura visual e ownership em caminhos separados, não criar v4/renderer paralelo e continuar falhando para a linha canônica onde não houver evidência territorial suficiente.
- **RISCO:** baixo/médio. O novo contrato é puro e não move a linha por conta própria sem consumidor, então não regressa a build atual; o risco seguinte está na integração do estado vivo: se um consumidor aplicar a mudança de ownership e ignorar `result.line`, a divergência continua; se aplicar a linha sem `sectorCaptureAllowed()`, pode recriar bolsões. Também não usar contatos de intel como ownership territorial.
- **COMMIT:** `df76071437c9ab7f058faa5d8a843561e2afbcc7` (captura + deformação canônica) + `3b16ae1de1719cfa59d1dea19e2e04559961aa9d` (regressão). Trabalho concorrente preservado no fechamento: `ddad3189afb9725f6ce97d5084c784e702579d09` + `3a9a0cef6c644d76fb7c3f0a703414dce831f751`; commit desta entrada é o commit atual.

### 2026-09-12 20:40 — ChatGPT-GPT-5.6-Sol / SLOT A — ARTILLERY
- **FEITO:** atualizada a visão da `iron-rain-v6-1-continuation`, relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md`, `AGENT_LOG.md`, commits recentes e, por preparar consumo na aba CARGAS de `table-map.js`, também `MAP_REWORK_20260912.md` + `MAP_REFERENCE_20260912.svg`. Continuada a cadeia existente `ballistics.js -> artillery-notebook.js -> artillery-crank-guide.js` sem recriar balística: criado `artilleryChargeTableRows()` como fronteira somente de apresentação para a aba CARGAS. Ela preserva as sete cargas/faixas e os valores de elevação, ápice e tempo da fonte compartilhada, mas acrescenta `MANIVELA ↑/↓/SEGURE` somente aos arcos da carga já inserida; cargas alternativas continuam apenas como referência e nenhuma carga/arco/elevação é escolhida ou aplicada automaticamente. Durante o fechamento entrou o commit concorrente `8fb4161b` endurecendo o retorno de câmera do projétil ao Mamute; foi inspecionado e preservado por tocar outro hotspot do mesmo workstream sem sobrepor esta seam.
- **ARQUIVOS:** `modules/artillery-charge-table-view.js`, `tests/artillery-charge-table-view.test.js`, `docs/AGENT_LOG.md`. Preservados `modules/ballistics.js`, `modules/artillery-notebook.js`, `modules/artillery-crank-guide.js` e `modules/table-map.js` neste ciclo. Consumidos `MAP_REWORK_20260912.md` + `MAP_REFERENCE_20260912.svg` antes de preparar a futura integração no mapa de mesa.
- **TESTE:** `node --check` passou para o novo módulo e `node --test` focal passou 3/3 com cópia dos módulos reais, cobrindo cues de manivela apenas na carga inserida, carga inserida inalcançável sem guidance inventado e imutabilidade profunda da fronteira de apresentação. A primeira expectativa de ápice/tempo foi corrigida para refletir exatamente `ballistics.js` (C5/30° = ápice 3.500 m e 53,4 s; arco alto 60° = ápice 10.500 m e 92,5 s), evitando cristalizar números manuais fora da fonte compartilhada. O `npm test` completo não foi declarado executado neste runtime.
- **PRÓXIMO:** após nova releitura de `table-map.js`, substituir o uso direto de `artilleryNotebookTableRows()` na aba CARGAS por `artilleryChargeTableRows(plot.distance, snapshot.charge, snapshot.elev)` e renderizar `arc.displayLabel`, mantendo BAIXO/ALTO simultaneamente quando ambos existirem. Fazer a integração como substituição da apresentação atual, não em paralelo, e preservar a ligação região/setor/intel parcial exigida pelo MAP REWORK sem revelar posição inimiga ou ponto de impacto.
- **RISCO:** baixo nesta fronteira pura/read-only; médio apenas na integração visual seguinte porque `table-map.js` é hotspot compartilhado com WORLD WAR. O principal risco é duplicar linhas/cues ou transformar `MANIVELA` em comando automático. A integração deve permanecer somente informativa e continuar usando `modules/ballistics.js` indiretamente como fonte única.
- **COMMIT:** `84c61555fd49f267474cef4cdec58df220fcbc63` (fronteira de apresentação) + `abafa71de3e498306cd672e485766ad1771844da` (regressão inicial) + `7d0a9d3d13d98a79b43c7d1c00af31a72842b577` (expectativas corrigidas). Trabalho concorrente preservado: `8fb4161beddb0a650b854aa03065b650b7574353`; commit desta entrada é o commit atual.

### 2026-09-12 20:30 — ChatGPT-GPT-5.6-Sol / SLOT A — COMBAT AI
- **FEITO:** atualizada a visão da `iron-rain-v6-1-continuation`, relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md`, `AGENT_LOG.md`, commits recentes e, por este trabalho consumir território/logística, também `MAP_REWORK_20260912.md` + `MAP_REFERENCE_20260912.svg`. Continuada a cadeia canônica de reservas sem recriar pathfinding, estoque, infraestrutura ou fonte de território: adicionado `combatReservePlanCycle()`, que compõe numa única chamada a rota real de `strategic-logistics`, os efeitos do território, o estado de fallback, o déficit da formação, o countdown e o reset do timer. Assim uma solicitação já devida continua em zero quando rota/território/fallback bloqueiam a chegada e só reinicia o intervalo quando um lote positivo é realmente admitido pelo mesmo snapshot canônico. Durante o fechamento entrou teste concorrente `474fdb9c` usando `createStrategicLogistics()` real; ele foi inspecionado e preservado por ser complementar, não duplicado.
- **ARQUIVOS:** `modules/combat-reserves.js`, `tests/combat-reserve-plan-cycle.test.js`, `tests/combat-reserve-plan-cycle-logistics.test.js` (concorrente preservado), `docs/AGENT_LOG.md`. Consumidos sem reescrever `modules/strategic-logistics.js`, `modules/territory-ai.js` ou `modules/war-simulation-core.js`.
- **TESTE:** adicionada regressão oficial cobrindo rota cortada mantendo `nextTimer=0`, admissão por depósito com `reinforcementSupport=.12` e lote `2.4`, reset customizado somente após admissão e território contestado falhando fechado sem consumir o pedido. Um harness focal equivalente em Node passou (`combat reserve plan cycle focal: ok`). O commit concorrente `474fdb9c` acrescentou cobertura com o grafo estratégico real, incluindo abertura/corte de rota via `setRouteOpen()` e endpoint de facção errada. O `npm test` completo continua indisponível neste runtime por DNS para GitHub, portanto a suíte integral local não foi declarada executada.
- **PRÓXIMO:** expor no estado/orquestração estratégica os IDs canônicos de origem/destino e o território correspondentes à formação; depois substituir atomicamente no `war-simulation-core.js` o bloco legado `3 + support.supply * 4` por `combatReservePlanCycle()`. Não manter o lote legado como fallback quando a seam canônica estiver presente e não resetar o timer quando a rota estiver cortada. Em ciclo separado, fechar o bypass de investida imediata para linha inimiga quase vazia usando o gate de recuperação/suprimento já existente.
- **RISCO:** baixo nesta composição pura/read-only; médio na integração real seguinte. `war-simulation-core.js` ainda materializa reforços pelo modelo local antigo, então o gameplay ainda possui a dívida de integração. O maior risco é migrar só tamanho ou só timer e deixar duas fontes de verdade; a troca precisa ser atômica quando os IDs canônicos estiverem disponíveis.
- **COMMIT:** `222a681a0c22a25132c271026e65a73f2fb8e473` (composição do ciclo) + `d1eaccdb0dedc0257753a7fe8986b3ff6d29cf03` (regressão). Trabalho concorrente preservado: `474fdb9c61763ff4b8721861fb6c46282071b783`; commit desta entrada é o commit atual.

### 2026-09-12 20:22 — ChatGPT-GPT-5.6-Sol-C / SLOT C — FP VISUALS + AUDIO
- **FEITO:** atualizada a visão da `iron-rain-v6-1-continuation` e relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md`, `AGENT_LOG.md` e commits recentes antes de editar. Durante o ciclo chegou trabalho concorrente no mesmo workstream normalizando impactos replicados por `remoteHullImpactFeedback()` e integrando-o em `cabin-view.js`; esse hotspot foi consumido/preservado, sem duplicação. Como o loader real continua preso ao hotspot grande `cabin-view-core.js` e o gate Codex ainda impede tocar `game-v6.js`, foi feita uma melhoria independente e pequena no feedback de manutenção já existente: `maintenance-overlay.js` agora respeita `prefers-reduced-motion: reduce`, removendo os loops contínuos de espuma/faíscas enquanto mantém os indicadores estáticos, progresso e semântica de reparo/extintor visíveis. Gameplay, timers, áudio, rede e progresso de manutenção não mudaram.
- **ARQUIVOS:** `modules/maintenance-overlay.js`, `tests/maintenance-overlay-motion.test.js`, `docs/AGENT_LOG.md`. Preservados `modules/cabin-view.js`, `modules/cabin-view-core.js`, `modules/remote-hull-impact-feedback.js`, `modules/maintenance-effect-cadence.js`, `modules/war-audio.js` e `game-v6.js`.
- **TESTE:** adicionada regressão oficial que fixa a media query de redução de movimento e garante que os pseudo-elementos de espuma/faíscas continuam visíveis sem `animation`. O checkout local/`npm test` continua indisponível por DNS para GitHub. O workflow `Publish Iron Rain` do descendente `6e94fa3a` terminou com `success`, portanto inclui os commits deste ciclo e executou a suíte/publicação da branch com sucesso. O workflow disparado diretamente por `abc37329` foi cancelado apenas por push concorrente posterior. A branch foi relida antes das escritas e o trabalho permaneceu ancestral/linear.
- **PRÓXIMO:** quando `cabin-view-core.js` estiver livre de concorrência, consumir `loaderRigState()` no renderer real e substituir a trajetória livre antiga de `loaderRound` por um único mecanismo articulado com posse garra → culatra; não sobrepor as duas apresentações. Quando o gate Codex liberar `game-v6.js`, integrar `loaderAudioCue()` nas transições reais substituindo o contato genérico em vez de somar transientes. Não fazer tuning subjetivo de ganho/cauda antes de evidência Safari/iPhone.
- **RISCO:** baixo. A mudança é apenas de apresentação/acessibilidade e preserva a informação visual de manutenção. O risco seguinte continua no loader: tocar `cabin-view-core.js` durante edição concorrente ou manter simultaneamente o shell livre e o rig articulado criaria duas apresentações; no áudio, manter `audio.load()` genérico junto das cues de fase duplicaria transientes.
- **COMMIT:** `a5c2e627bfb4863dcfc56f1da5c75ba4c8b17e94` (reduced motion no overlay) + `abc37329b5e72c01c6eb556f5aa5cc40dc9a3dd0` (regressão); commit desta entrada é o commit atual.

### 2026-09-12 20:19 — ChatGPT-GPT-5.6-Sol / SLOT A — FP VISUALS + AUDIO
- **FEITO:** atualizada a visão da `iron-rain-v6-1-continuation` e relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md`, `AGENT_LOG.md`, commits recentes, `CODEX_LIVE_DISPATCH_20260912.md` e o pedido Codex P0 ativo antes de editar. Durante a leitura entraram os commits concorrentes `e05a0cfe` + `80cae62e` corrigindo a cadência de manutenção após reset de clock; foram preservados e não duplicados. Como o Codex continua `IN_PROGRESS`, `game-v6.js`, transporte, signaling e autoridade permaneceram intactos. Foi criada `remoteHullImpactFeedback()` como adaptador puramente de apresentação sobre o contrato já canônico `hullImpactFeedback()`: impactos replicados agora derivam força de shake/feedback da mesma combinação fictícia de dano + tipo de contato usada pelo casco local, preservam `payload.intensity` quando válido e mantêm `critical` em intensidade total. `cabin-view.js` passou a consumir esse adaptador no evento compartilhado em vez do fallback visual fixo `.65/1`, portanto rifle/HMG e impacto pesado deixam de parecer iguais sem criar nova regra de dano, rede ou timer.
- **ARQUIVOS:** `modules/remote-hull-impact-feedback.js`, `modules/cabin-view.js`, `tests/remote-hull-impact-feedback.test.js`, `docs/AGENT_LOG.md`. Consumido sem reescrever `modules/cabin-hit-feedback.js`; preservados `modules/war-audio.js`, `modules/integration-live.js`, `modules/maintenance-effect-cadence.js`, `game-v6.js` e toda a autoridade/transport multiplayer.
- **TESTE:** `node --check` passou no helper e `node --test` focal passou 3/3 cobrindo escala rifle→tanque pela mesma fonte canônica, `critical` sempre em intensidade total, preservação de intensidade transmitida válida e rejeição de eventos que não são impacto. O checkout/`npm test` completo continua indisponível neste runtime por DNS para `github.com`, então a suíte integral não foi declarada executada. A branch foi relida imediatamente antes das escritas e após módulo+teste+consumidor; permaneceu linear em `96af8473` antes deste handoff.
- **PRÓXIMO:** após nova releitura dos hotspots, fazer `integration-live.js` normalizar também o payload entregue a `war-audio.impact()` pelo mesmo `remoteHullImpactFeedback()` (ou por uma seam compartilhada equivalente), evitando áudio remoto cair em defaults enquanto o shake já conhece dano/tipo; não duplicar o evento nem tocar em transporte. Separadamente, quando o gate Codex liberar `game-v6.js`, consumir `loaderAudioCue()` nas transições reais e substituir o contato genérico por fase em vez de somar sons.
- **RISCO:** baixo. A mudança é somente de apresentação e não altera HP, dano, simulação, autoridade ou tráfego. O principal cuidado seguinte é não calcular a normalização duas vezes com resultados divergentes entre áudio e visual; ambos devem consumir um único contrato. Também não fazer tuning subjetivo de ganho, luz ou cauda antes de evidência real Safari/iPhone.
- **COMMIT:** `117ea2b2d5c4f508dca293a694e93cdbdfcf519d` (normalização), `1e1deceb64ffb3876abd2501f64a0a7d2cd3dc3f` (regressão) + `96af84732369086340c1bb5750fe1f1a4e7b6f6a` (consumo visual); commit desta entrada é o commit atual.

### 2026-09-12 20:07 — ChatGPT-GPT-5.6-Sol-B / SLOT B — FP SYSTEMS
- **FEITO:** atualizada a visão da `iron-rain-v6-1-continuation` e relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md`, `AGENT_LOG.md`, commits recentes, `CODEX_LIVE_DISPATCH_20260912.md` e o pedido Codex P0 ativo antes de editar. O Codex permanece `IN_PROGRESS`, então `game-v6.js`, transporte, signaling, lobby e autoridade de sessão foram preservados. Continuado o lifecycle de postos já existente com uma correção isolada no bridge: quando um guest já está `pending-host` e o host responde `crew-station-deny`, o runtime canônico publica `lastEvent=station-denied:<posto>:<owner>` sem necessariamente atualizar ownership; o bridge agora consome esse evento somente para o posto pendente, encerra o estado pending e traduz para `occupied` quando há owner ou `claim-denied` quando não há. Assim a interação não fica presa para sempre em “aguardando confirmação”, e uma tentativa posterior pode reenviar o claim depois que a negação foi consumida. Nenhuma segunda autoridade, timer de ACK ou polling de transporte foi criado.
- **ARQUIVOS:** `modules/crew-cabin-bridge.js`, `tests/crew-cabin-bridge.test.js`, `docs/AGENT_LOG.md`. Preservados `modules/crew-station-gate.js`, `modules/crew-runtime.js`, `modules/crew-session.js`, `bootstrap.js`, `game-v6.js`, `modules/cabin-controls.js` e `modules/cabin-view.js`.
- **TESTE:** `node --check` passou para a cópia focal do bridge e um harness em Node passou cobrindo claim guest pendente -> denial com owner -> feedback `occupied` -> nova tentativa permitida, além de denial sem owner -> `claim-denied`. Foi adicionada regressão oficial equivalente em `crew-cabin-bridge.test.js`. O checkout/`npm test` completo continua indisponível neste runtime por DNS (`Temporary failure in name resolution` para `raw.githubusercontent.com`/GitHub), portanto a suíte local completa não foi declarada executada. A branch foi relida imediatamente antes do primeiro commit, após módulo+teste e antes deste handoff; permaneceu linear.
- **PRÓXIMO:** enquanto o Codex P0 continuar `IN_PROGRESS`, continuar apenas preparação independente de FP SYSTEMS. Se o Codex retornar `DONE`, consumir primeiro o adapter/evidência devolvidos e então integrar `bridge.requestStation()`/`bridge.releaseStation()` no `enterStation()`/`leaveStation()` real: só assumir localmente quando `ready`, manter pending fora do posto, mostrar denial/occupied e liberar autoridade antes de limpar estado local. Se o Codex retornar `BLOCKED`, seguir a seam mínima indicada por ele, sem inventar transporte concorrente.
- **RISCO:** baixo. A mudança observa apenas `runtime.status().lastEvent` para resolver um claim que o próprio bridge sabe estar pendente; não concede posse nem altera session state. O risco futuro é usar `lastEvent` como autoridade geral — não fazer isso; ownership continua vindo exclusivamente de `stationOwner()`/runtime. Também não integrar o hotspot real de `game-v6.js` antes do gate Codex liberar.
- **COMMIT:** `30a87f87eced028eac929f647728a6469024c3b6` (resolver negação de claim pendente) + `7e3e9248f3974a9b64e62c515befbd99576768b3` (regressão); commit desta entrada é o commit atual.

### 2026-09-12 20:03 — ChatGPT-GPT-5.6-Sol / SLOT A — FP SYSTEMS
- **FEITO:** atualizada a visão da branch ativa e relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md`, `AGENT_LOG.md`, commits recentes e o gate `CODEX_LIVE_DISPATCH_20260912.md` antes de editar. Continuado o contrato já existente `crew-station-gate.js` -> `crew-cabin-bridge.js` sem tocar em transporte, signaling, lobby, `crew-runtime.js` ou `game-v6.js`. O bridge agora mantém apenas o estado de apresentação de um claim guest que já retornou `pending-host`: leituras seguintes de `stationState()` continuam bloqueadas/pending até o runtime canônico confirmar ownership, reportar ocupação/desconexão ou o posto ser liberado. Repetir a interação enquanto o ACK está pendente não dispara claims duplicados. O estado pendente é limpo em `clear()` e `releaseStation()`. Autoridade continua exclusivamente no runtime/session canônico.
- **ARQUIVOS:** `modules/crew-cabin-bridge.js`, `tests/crew-cabin-bridge.test.js`, `docs/AGENT_LOG.md`. Preservados `modules/crew-station-gate.js`, `modules/crew-runtime.js`, `modules/crew-session.js`, `modules/cabin-controls.js`, `modules/cabin-view.js` e `game-v6.js`.
- **TESTE:** adicionado teste oficial cobrindo persistência de `pending-host`, ausência de retransmissão de claim durante ACK pendente, transição automática para `owned` quando o runtime passa a reportar o jogador local como dono e limpeza/fail-closed ao desconectar. Um harness focal equivalente em Node passou (`crew cabin pending station focal: ok`). O checkout/`npm test` completo continua indisponível neste runtime porque `git clone` falha com `Could not resolve host: github.com`. A branch foi relida antes do primeiro commit e novamente após módulo+teste; entrou apenas um commit concorrente de LEAD em `strategic-war-live.js`, descendente dos commits deste ciclo e sem sobreposição com FP SYSTEMS.
- **PRÓXIMO:** quando o gate Codex P0 deixar `IN_PROGRESS` ou o hotspot real for liberado, consumir `bridge.requestStation()` no fluxo real de `enterStation()` e somente entrar quando `ready`; enquanto `pending`, manter o jogador fisicamente fora do posto e exibir `bridge.stationMessage()`. Ao sair/cancelar, chamar `bridge.releaseStation()` antes de limpar o estado local. Não criar polling/ACK paralelo: `stationState()` deve continuar observando ownership do runtime canônico.
- **RISCO:** baixo. A mudança não concede posse nem altera gameplay sem consumidor; ela só impede a UI de voltar falsamente a `available` durante um claim guest em voo. O principal risco futuro é esquecer de chamar `clear()`/`releaseStation()` em uma troca de sessão ou integrar o consumidor real enquanto Codex ainda possuir o hotspot de transporte, criando duas fontes de lifecycle.
- **COMMIT:** `c73e0322109edfca33f9d7a6784239159378e76b` (persistência de pending no bridge) + `d09461ca308daf2f60f39dc063fb9ca7ae45166b` (regressão); commit desta entrada é o commit atual.

### 2026-09-12 19:50 — ChatGPT-GPT-5.6-Sol / SLOT A — WORLD WAR
- **FEITO:** atualizada a visão da branch ativa e relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md`, `AGENT_LOG.md`, commits recentes, `MAP_REWORK_20260912.md` e `MAP_REFERENCE_20260912.svg` antes de editar. O renderer canônico v3 ainda desenha a frente por `lineSnapshot()` estático enquanto a malha de setores já possui ownership capturável. Foi criada `strategicFrontPressure()` como fronteira pura/read-only que resume por faixas horizontais a borda mais avançada ALIADA e a borda mais avançada EIXO, produzindo o ponto médio somente quando existe corredor territorial coerente (`allyEdge <= enemyEdge`). Faixas sem evidência dos dois lados ou com ownership cruzado falham para a linha central, evitando inventar bolsões/teleporte de frente. Não foi criado renderer paralelo nem alterada a fonte canônica de território.
- **ARQUIVOS:** `modules/strategic-front-pressure.js`, `tests/strategic-front-pressure.test.js`, `docs/AGENT_LOG.md`. Consumidos como referência sem reescrever `modules/strategic-hex-map.js`, `modules/strategic-war-live-v3.js` ou `modules/theatre-control.js` neste ciclo. Confirmado consumo obrigatório de `MAP_REWORK_20260912.md` + `MAP_REFERENCE_20260912.svg`.
- **TESTE:** `node --check` passou e `node --test` focal passou 3/3 cobrindo curvatura pelo espaço real entre bordas aliada/inimiga, fallback central para faixa sem suporte ou com ownership cruzado e dimensões inválidas fail-closed. O checkout/`npm test` completo continua indisponível neste runtime por DNS para `github.com`. A branch foi relida imediatamente antes do primeiro commit, entre módulo/teste e antes deste handoff; permaneceu linear.
- **PRÓXIMO:** integrar `strategicFrontPressure()` diretamente em `strategic-war-live-v3.js` como deformação limitada sobre `lineSnapshot()` (não substituto de renderer), alimentando-a somente com `theatre.records` canônicos. A deformação deve ser suavizada/limitada por distância para preservar a frente contínua e nunca aceitar posição inimiga fora do ownership/intel já conhecido. Depois ligar captura legítima via `sectorCaptureAllowed()` ao mesmo estado territorial para que ownership e curva visual deixem de divergir.
- **RISCO:** baixo enquanto o helper permanece puro; médio na integração visual seguinte. Se o renderer usar os samples como nova fonte absoluta, pode romper o baseline/neutralidade; deve continuar consumindo o teatro existente e apenas curvar a linha. Não usar contatos de intel como ownership nem converter ausência de informação em território inimigo conhecido.
- **COMMIT:** `6345b01db1c537faf3913e6ea0476c5d39e4e941` (pressão territorial por faixas) + `81050f23a7b5c19210a6d91907c581b45845e5b8` (regressão); commit desta entrada é o commit atual.

### 2026-09-12 19:42 — ChatGPT-GPT-5.6-Sol / SLOT A — ARTILLERY
- **FEITO:** atualizada a visão da branch ativa, relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md`, `AGENT_LOG.md`, o HEAD e os commits recentes antes de editar. Continuado o guia manual de manivela existente sem alterar `modules/ballistics.js`: a tolerância de `SEGURE` agora acompanha a resolução realmente mostrada ao operador (0,1°), evitando instruções contraditórias como `↑ 0.0°`/`↓ 0.0°` para diferenças subvisuais. O `delta` balístico bruto continua preservado e nenhuma carga, arco ou elevação é escolhida/aplicada automaticamente. A primeira tentativa de registrar este handoff foi recusada por conflito de SHA; o log foi relido e a entrada concorrente de COMBAT AI das 19:31 foi preservada antes desta nova escrita.
- **ARQUIVOS:** `modules/artillery-crank-guide.js`, `tests/artillery-crank-guide.test.js`, `docs/AGENT_LOG.md`. Preservados `modules/ballistics.js`, `modules/artillery-notebook.js` e `modules/table-map.js` neste ciclo.
- **TESTE:** adicionada regressão cobrindo diferença de `-0.04°` como `hold/SEGURE` e diferença de `-0.06°` como correção visível `↓ 0.1°`, preservando os casos anteriores de arco baixo/alto, ápice, carga inalcançável e entrada inválida. Como o checkout completo continua bloqueado por DNS (`Could not resolve host: github.com`), foi executado um harness focal em Node com cópia dos módulos reais; `node --check` passou e o harness imprimiu `artillery crank display tolerance focal: ok`. A branch avançou concorrentemente após os commits deste ciclo; `2800d123` permaneceu ancestral e os commits posteriores observados ficaram fora de `artillery-crank-guide.js`/seu teste.
- **PRÓXIMO:** após nova releitura do hotspot compartilhado, consumir `instructionLabel`/`movementLabel` na aba CARGAS de `table-map.js` para a carga inserida, mantendo BAIXO/ALTO simultaneamente quando ambos existirem e sem selecionar arco, mover a peça ou revelar impacto. Coordenar com WORLD WAR antes de editar `table-map.js`, pois o mesmo arquivo recebe integração de intel parcial.
- **RISCO:** baixo. A mudança altera somente o feedback de manivela na escala exibida; o valor bruto de `delta`, as soluções da caderneta e a física fictícia compartilhada não mudam. O cuidado seguinte é não transformar a caderneta em automação de tiro e não sobrescrever integração concorrente de World War em `table-map.js`.
- **COMMIT:** `888533d8589d470670c7eb97f5bbdd14c3c81a82` (tolerância alinhada à exibição) + `2800d12319fb90028ce39755b2677c59f2095047` (regressão); commit desta entrada é o commit atual.

### 2026-09-12 19:31 — ChatGPT-GPT-5.6-Sol-C / SLOT C — COMBAT AI
- **FEITO:** atualizada a visão da branch e relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md`, `AGENT_LOG.md` e os commits recentes antes de editar. O primeiro write foi corretamente recusado por conflito de SHA porque outro agente havia acabado de compor `combatReservePlan()`; a branch foi relida e esse trabalho foi preservado. Sobre a cadeia canônica existente foi adicionado `combatReserveCycle()`: o countdown ainda avança normalmente, mas quando uma reposição já está devida e rota/fallback bloqueiam a chegada o timer permanece em zero, sem consumir nem adiar artificialmente o pedido. Somente um lote positivo realmente admitido reinicia o intervalo. Nenhum pathfinding, estoque, infraestrutura ou fonte de logística paralela foi criado.
- **ARQUIVOS:** `modules/combat-reserves.js`, `tests/combat-reserve-cycle.test.js`, `docs/AGENT_LOG.md`. Consumidos sem reescrever `modules/strategic-logistics.js`, `modules/territory-ai.js`, `modules/war-simulation-core.js` ou o `combatReservePlan()` concorrente.
- **TESTE:** `node --check` passou no helper/harness focal e as quatro asserções executadas em Node passaram: countdown antes do vencimento, rota cortada preservando `nextTimer=0`, fallback incompleto preservando o pedido devido e reset do intervalo somente após lote positivo. O checkout/`npm test` completo continua indisponível neste runtime por DNS para `github.com`, portanto a suíte oficial não foi declarada executada. A branch foi relida entre as escritas e permaneceu linear.
- **PRÓXIMO:** integrar incrementalmente a cadeia `combatReservePlan()` + `combatReserveCycle()` no consumidor real de reforços, substituindo o legado `3 + support.supply * 4` somente quando a orquestração puder fornecer o `strategicLogistics`, território e endpoints canônicos da formação. Não criar um segundo mapa/logística só para satisfazer o helper; se essa seam ainda não existir, expô-la pela orquestração estratégica existente em mudança pequena antes de tocar no bloco de materialização.
- **RISCO:** baixo enquanto o ciclo permanece helper puro; médio na integração seguinte. `war-simulation-core.js` ainda materializa reservas pela camada local antiga, então estes contratos ainda não mudam gameplay. O maior risco é ligar apenas tamanho/gate sem o estado estratégico real ou resetar o timer quando a rota está cortada, recriando duas fontes de verdade.
- **COMMIT:** `6488c5acb20196eb7a833af6abf050a62e10fdc8` (persistência do timer devido) + `2ba6335e7de59c9362934a56f2d49c840fc91f1b` (regressão); commit desta entrada é o commit atual.

### 2026-09-12 19:30 — ChatGPT-GPT-5.6-Sol / SLOT A — COMBAT AI
- **FEITO:** atualizada a visão da branch ativa, relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md`, `AGENT_LOG.md`, o HEAD e os commits recentes antes de editar. Consumido o trabalho recém-chegado que compôs `combatReserveDecision()` em vez de recriar gate/lote. Adicionado `combatReservePlan()` em `combat-reserves.js`: uma única composição read-only consulta a reachability do grafo estratégico existente via `combatRouteOpen()`, deriva o snapshot territorial via `combatLogisticsState()` e executa a decisão de admissão+tamanho via `combatReserveDecision()`. Assim a futura integração em `war-simulation-core.js` pode usar uma única chamada e não correr o risco de validar rota com um snapshot e calcular o lote com outro; nenhum pathfinding, estoque, construção ou bônus paralelo foi criado.
- **ARQUIVOS:** `modules/combat-reserves.js`, `tests/combat-reserve-plan.test.js`, `docs/AGENT_LOG.md`. Preservados `modules/strategic-logistics.js`, `modules/territory-ai.js`, `modules/war-simulation.js` e `modules/war-simulation-core.js` neste ciclo.
- **TESTE:** adicionada regressão oficial cobrindo rota aliada válida + depósito (`reinforcementSupport=.12`, lote 2.4), rota canônica cortada e endpoint hostil rejeitado. O checkout/`npm test` completo continua bloqueado neste runtime por DNS (`Could not resolve host: github.com`). Um harness focal equivalente em Node passou após `node --check`, imprimindo `combat reserve plan focal: ok`. A branch foi relida imediatamente antes das escritas e novamente depois dos commits; permaneceu linear em `c0827cfd01b3ffea5b23dbf85c7d4dabfa1c885f` antes desta entrada.
- **PRÓXIMO:** integrar `combatReservePlan()` no timer real de reforços em `war-simulation-core.js`, substituindo atomicamente o legado `Math.min(100 - sec[key], 3 + support.supply * 4)` somente quando houver IDs canônicos de território/origem/destino disponíveis no estado; enquanto rota/território não estiverem disponíveis, falhar fechado em vez de manter duas fontes de reforço. Em ciclo separado, fechar o bypass de investida imediata para linha inimiga quase vazia usando o mesmo gate de recuperação/suprimento, sem duplicar thresholds.
- **RISCO:** baixo no helper composto porque é puro/read-only e apenas encadeia fontes já canônicas. O risco funcional continua médio na integração do hotspot: `war-simulation-core.js` ainda usa `assetSupport()/base.supply` para munição e o lote legado de reforços; migrar só metade do fluxo criaria duas fontes de verdade. Não resetar o timer quando o plano estiver bloqueado por rota/fallback/infraestrutura e não derivar rotas novas dentro de Combat AI.
- **COMMIT:** `96a3da2e0aaa80064914bad26a91b3835d2fc0f1` (composição canônica) + `c0827cfd01b3ffea5b23dbf85c7d4dabfa1c885f` (regressão); commit desta entrada é o commit atual.

### 2026-09-12 19:15 — ChatGPT-GPT-5.6-Sol / SLOT A — FP VISUALS + AUDIO
- **FEITO:** atualizada a visão da branch e relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md`, `AGENT_LOG.md` e os commits recentes antes de editar. Consumida a integração recém-chegada de efeitos compartilhados em `integration-live.js` sem criar outra camada de áudio/rede. Foi corrigido um custo real do feedback de manutenção: eventos de progresso de reparo/extintor podiam ser replicados na frequência do produtor e cada remoto convertia cada evento em um transiente WebAudio. Criado um gate pequeno de apresentação, imediatamente consumido por `integration-live.js`, que mantém início, troca de ferramenta e conclusão imediatos, mas limita eventos intermediários a um intervalo de 0,28 s. Gameplay, progresso, autoridade e transporte não foram alterados; `war-audio.js` e seus buses/limite de vozes permanecem a fonte de playback.
- **ARQUIVOS:** `modules/maintenance-effect-cadence.js`, `modules/integration-live.js`, `tests/maintenance-effect-cadence.test.js`, `docs/AGENT_LOG.md`. Preservados `modules/war-audio.js`, `modules/engine-system.js`, `modules/loader-audio-cue.js` e o restante da integração multiplayer.
- **TESTE:** `node --check` passou para a cópia focal do helper e `node --test` passou 2/2 casos equivalentes: início imediato, supressão de spam dentro da janela, emissão periódica em manutenção longa, troca repair→extinguisher imediata, conclusão emitida uma vez, clamp de progresso e rejeição de feedback irrelevante/clock inválido. O checkout/`npm test` completo continua indisponível neste runtime por DNS para `github.com`. A branch foi relida antes da integração e novamente antes deste handoff; permaneceu linear nos commits deste ciclo.
- **PRÓXIMO:** integrar a semântica já existente de `loaderAudioCue()` no playback real por fase (`clamp/swing/ram/lock`) somente após nova releitura de `game-v6.js`/`war-audio.js`, substituindo o contato genérico em vez de somar dois sons. Em manutenção, se QA real mostrar que reparo/extintor ainda soam iguais, criar timbres distintos reutilizando os mesmos buses/buffer e sem aumentar voz contínua; não fazer tuning subjetivo de ganho/cauda antes de evidência Safari/iPhone.
- **RISCO:** baixo. O gate afeta somente a frequência de feedback remoto transitório e não o progresso da manutenção. O principal cuidado é não usar esse cadence como timer de gameplay e não duplicar throttle em produtores/consumidores diferentes; `integration-live.js` deve continuar sendo o único consumidor deste gate para efeitos compartilhados.
- **COMMIT:** `614e299aca333fbb8a87fce55b7a4b051e24015a` (cadência), `fac1c4bdd6785407639ce3d6b48b03c2f6fe9279` (integração) + `1fd23fb1ebccb1075dd710ab18470d5082639f93` (regressão); commit desta entrada é o commit atual.

### 2026-09-12 18:32 — ChatGPT-GPT-5.6-Sol-C / SLOT C — COMBAT AI
- **FEITO:** atualizada a visão da branch e relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md` e `AGENT_LOG.md` antes de editar. Consumida a fundação já existente de `combat-reserves.js`/`strategic-logistics.js` sem criar pathfinding, estoque ou infraestrutura paralelos. Adicionado `combatRouteOpen()`, que valida endpoints aliados/inimigos e delega a reachability integralmente ao `strategic-logistics.route()`: caminhos multi-hop válidos abrem a chegada de reservas e cortes reais de rota fecham imediatamente o gate; entradas inválidas falham fechado. Durante o fechamento outro agente consumiu esta mesma fundação e acrescentou `combatReserveBatch()` + regressões de tamanho de lote escalado pelo suporte logístico, trabalho preservado sem reescrita.
- **ARQUIVOS:** `modules/combat-reserves.js`, `tests/combat-reserves.test.js`, `docs/AGENT_LOG.md`. Consumidos sem reescrever `modules/strategic-logistics.js`, `modules/territory-ai.js` ou o hotspot `modules/war-simulation.js`.
- **TESTE:** `node --check` passou na cópia focal do módulo/teste e `node --test` passou 2/2 casos equivalentes cobrindo caminho multi-hop, corte de rota, endpoint de facção errada, endpoint ausente, adapter inválido e mesmo nó válido. O checkout local/`npm test` continua indisponível neste runtime por DNS, então a regressão não pôde ser executada aqui; a implementação/teste foram revisados estaticamente. A branch foi relida imediatamente antes das escritas e novamente antes deste handoff; permaneceu linear nos commits deste ciclo.
- **PRÓXIMO:** somente após nova releitura do hotspot, integrar em `war-simulation.js` uma cadeia única `strategic-logistics -> combatRouteOpen() -> combatLogisticsState() -> combatReserveGate()/combatReserveBatch()`: quando a rota estiver cortada, o território local estiver contestado ou não houver outpost/depot/garage realmente construído, o timer não deve materializar reposição; quando estiver pronto, o lote deve usar o suporte territorial já ganho em vez do legado `3 + support.supply * 4`. Preservar `combatRecoveryPhase()` e migrar incrementalmente, sem manter dois modelos de reservas ativos.
- **RISCO:** baixo nesta fronteira pura; médio na integração seguinte porque `war-simulation.js` ainda usa `assetSupport()/base.supply` para munição/reforços. Conectar apenas parte do fluxo pode criar duas fontes de verdade ou deixar o timer consumir/adiar reservas de forma incoerente. O novo `combatReserveBatch()` concorrente deve ser consumido junto do gate, não em caminho paralelo.
- **COMMIT:** `6ae41753abe518fa7c350cc61959776220318ed6` (route reachability) + `5616a25c92dafcf581b79d203c001df736d1cf9c` (regressões). Trabalho concorrente preservado: `2069a7482853f5407a20a594fce355f2912625fd` + `5f59f7dff4e55c8e18def3f58f9982e2b39e70a8`; commit desta entrada é o commit atual.

### 2026-09-12 18:05 — ChatGPT-GPT-5.6-Sol-B / SLOT B — FP SYSTEMS
- **FEITO:** atualizada a visão da branch e relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md`, `AGENT_LOG.md` e os commits recentes antes de editar. Consumido sem duplicação o `crew-station-gate.js` recém-integrado: `createCrewCabinBridge()` agora expõe `stationState()`, `requestStation()` e `releaseStation()` delegando ao gate canônico, de modo que a mesma fronteira runtime → cabine que já publica pose/presença também concentra a posse de posto. Single-player permanece imediato; host usa o runtime canônico; guest `pending`/`occupied` permanece bloqueado sem entrada local antecipada. Não foi criado transporte/lobby paralelo e `game-v6.js` não foi alterado neste ciclo.
- **ARQUIVOS:** `modules/crew-cabin-bridge.js`, `tests/crew-cabin-bridge.test.js`, `docs/AGENT_LOG.md`. Consumidos sem reescrever `modules/crew-station-gate.js`, `modules/crew-runtime.js`, `modules/crew-session.js`, `modules/cabin-view.js` ou `game-v6.js`.
- **TESTE:** checkout/`npm test` completo indisponível neste runtime por DNS (`Could not resolve host: github.com`). `node --check` passou no bridge/harness focal e um harness equivalente em Node passou 3/3 cobrindo single-player, claim/release de host e guest pending/occupied; o teste oficial foi ampliado com os mesmos contratos. A branch foi relida imediatamente antes das escritas, entre os commits e antes deste handoff; permaneceu linear.
- **PRÓXIMO:** integrar esta fronteira no fluxo real de interação de `game-v6.js` somente após nova releitura do hotspot: `enterStation()` deve pedir `bridge.requestStation(id)` e só entrar quando `ready`; guest `pending` aguarda confirmação do host e `occupied` deve receber feedback sem assumir o posto; `leaveStation()` deve liberar via `bridge.releaseStation()` antes de limpar o estado local. Não criar transporte concorrente enquanto o P0 de multiplayer real já delegado permanecer ativo.
- **RISCO:** baixo nesta ponte porque ela não altera gameplay sem consumidor e preserva o caminho offline. O principal risco seguinte é `game-v6.js` manter posse local diretamente em paralelo ao runtime, ou um guest entrar visualmente antes do ACK do host, criando duas fontes de verdade para estação.
- **COMMIT:** `4dcefbdfd2c9eb14ded9493aa4e0d74d5bf98efc` (ponte de autoridade) + `410f994a1db557e31fe3d18561f279251c0adc50` (regressões); commit desta entrada é o commit atual.

### 2026-09-12 17:52 — ChatGPT-GPT-5.6-Sol-C / SLOT C — WORLD WAR
- **FEITO:** atualizada a visão da branch e relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md` e `AGENT_LOG.md` antes de editar. Consumida sem duplicação a fundação recém-chegada `theatre-mamutes.js`, que cria o roster canônico de Mamutes das duas facções. Criado `theatre-mamute-map.js` para compor esse roster com a política de informação parcial já existente: Mamutes aliados podem usar sua posição canônica no mapa da própria facção, mas Mamutes inimigos só aparecem quando existe intel já conquistada com o mesmo id; coordenadas inimigas nunca são lidas do roster para renderização do jogador, e contato `lost` permanece apenas textual com `x/y=null`. Assim múltiplos Mamutes entram no teatro compartilhado sem transformar o mapa em sensor onisciente.
- **ARQUIVOS:** `modules/theatre-mamute-map.js`, `tests/theatre-mamute-map.test.js`, `docs/AGENT_LOG.md`. Consumidos sem reescrever `modules/theatre-mamutes.js`, `modules/intel-map-snapshot.js`, `modules/intel-knowledge.js` ou `modules/table-map.js`.
- **TESTE:** adicionada regressão focal cobrindo posição aliada exata, inimigo revelado somente por intel, inimigo sem intel completamente ausente, `lost` sem coordenadas, rejeição fail-closed de intel inválida e objetos congelados. O checkout local/`npm test` continua indisponível neste runtime por DNS, então a regressão não pôde ser executada aqui; a implementação/teste foram revisados estaticamente. A branch foi relida imediatamente antes das escritas e novamente antes deste handoff; permaneceu linear nos commits deste ciclo.
- **PRÓXIMO:** integrar `buildTheatreMamuteMapContacts()` no view-model do mapa quando `table-map.js` estiver estável, alimentando inimigos exclusivamente com `buildIntelMapSnapshot()` e aliados com o roster canônico. Depois, ligar atualização do roster à orquestração estratégica compartilhada sem amarrar existência do Mamute à sessão de tripulação. Não mostrar `crewCount`, munição ou posição canônica inimiga como atalho de UI.
- **RISCO:** baixo enquanto o helper está isolado. O risco da integração seguinte é manter simultaneamente marcadores inimigos vindos do roster e do ledger, reintroduzindo onisciência; `table-map.js` deve receber apenas o contato composto. Também é preciso decidir de forma explícita como relatórios de rádio identificam um Mamute canônico quando ainda não houver `id` compartilhado, sem recorrer a busca por posição viva.
- **COMMIT:** `ebe037360f9619688b738640ffe989dfa3a89e49` (gate de contatos do mapa) + `69974a26d239164bcf28bf94e01d4e2a3a23c45f` (regressão); commit desta entrada é o commit atual.

### 2026-09-12 17:19 — ChatGPT-GPT-5.6-Sol-C / SLOT C — FP VISUALS + AUDIO
- **FEITO:** atualizada a visão da branch e relidos `GITHUB_HANDOFF.md`, `MULTI_AGENT_CONTROL.md`, `AGENT_LOG.md`, sprint ativo e diretivas atuais antes de escolher trabalho. Consumida sem duplicação a mudança concorrente recém-chegada do loader que adicionou dwell real no lock antes da retração. Como `cabin-view-core.js` continua com a apresentação antiga de `loaderRound` livre e o renderer/game loop seguem hotspots concorridos, foi evitada uma integração grande nesse ciclo. Em vez disso, criado `loader-audio-cue.js`, derivado exclusivamente de `loaderActivity()`/clock já existente, para distinguir semanticamente transições reais da máquina (`clamp`, `swing`, `ram`, `lock`) sem segundo timer, sem estado paralelo e sem alterar mix/gain subjetivamente. O QA iPhone/Safari continua necessário e não foi declarado resolvido.
- **ARQUIVOS:** `modules/loader-audio-cue.js`, `tests/loader-audio-cue.test.js`, `docs/AGENT_LOG.md`. Consumidos sem reescrever `modules/loader-arm.js`, `modules/war-audio.js`, `game-v6.js` ou `modules/cabin-view-core.js`.
- **TESTE:** validação focal em Node passou 3/3 casos e `node --check` passou para módulo/teste: uma cue por mudança real de fase, nenhum retrigger enquanto a fase permanece igual, nenhum evento após ciclo completo e flags `heavyMotion`/`rammer` idênticos aos de `loaderActivity()`. `npm test` completo não pôde ser executado porque o runtime continua sem resolver `github.com` para checkout (`Could not resolve host: github.com`). A branch foi relida imediatamente antes dos commits e novamente antes deste handoff; o commit concorrente de Slot A foi preservado.
- **PRÓXIMO:** integrar `loaderAudioCue()` no caminho real de transição de `state.loading.phase` em `game-v6.js` e implementar playback procedural por fase em `war-audio.js`, reutilizando buses/buffer/nós transitórios existentes em vez de criar clock/loop de áudio próprio. Separadamente, quando o hotspot visual estiver seguro, consumir `loaderRigState()` em `cabin-view-core.js` para substituir a trajetória livre antiga de `loaderRound` por um único braço articulado com posse garra → culatra; não sobrepor as duas apresentações. Não ajustar ganho/cauda subjetivamente antes de QA real em Safari/iPhone.
- **RISCO:** baixo nesta fundação porque ainda não altera playback nem gameplay; a cue é pura e depende do estado mecânico já canônico. O risco da integração seguinte é manter simultaneamente `audio.load()` genérico e cues de fase, duplicando transientes, ou criar um segundo estado de recarga no áudio. No visual, o risco continua ser deixar `loaderRound` livre junto do novo braço em vez de substituí-lo.
- **COMMIT:** `6514f153a7a595d51530a5aedc5953660a324d02` (contrato de cues) + `6ef614e53f9abb0277977a02fe62f97190684e0f` (regressão); commit desta entrada é o commit atual.