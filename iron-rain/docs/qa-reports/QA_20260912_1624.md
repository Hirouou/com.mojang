# Iron Rain — QA Governance — 2026-09-12 16:24 BRT

Branch revisada: `iron-rain-v6-1-continuation`.
Branch histórica preservada: `iron-rain-frontline`.

## Escopo revisado
- `docs/GITHUB_HANDOFF.md`
- `docs/MULTI_AGENT_CONTROL.md`
- `docs/AGENT_LOG.md`
- commits recentes da branch
- `game-v6.js`
- `modules/war-audio.js` / regressão de wake mobile
- `modules/crew-lobby-ui.js`
- fundações recentes de guerra persistente / linha territorial
- fila Codex, especialmente `IR-CODEX-20260912-1518-LEAD-ios-mobile-audio.md`

## Resultado

### 1. Lobby de tripulação: fundação boa, ainda NÃO visível na build principal
Os commits recentes adicionam `crew-lobby-ui.js` e estilos responsivos para desktop/mobile, com host/join, código do Mamute, lista de até 3 tripulantes e entrada offline. Porém o `game-v6.js` atual não importa nem instancia `createCrewLobbyUI()`. Portanto, por QA, esta mudança deve ser tratada como infraestrutura isolada e não como multiplayer/lobby entregue ao jogador.

**Risco:** baixo enquanto isolado; médio quando integrar, porque a UI precisa consumir uma única sessão/transport authority e não criar uma segunda fonte de estado de tripulação.

### 2. Áudio mobile: correção coerente, mas bug de iPhone ainda não está fechado
A mudança em `war-audio.js` move `primeMobileOutput()` para depois da trava `resumePending`, evitando criar fontes de priming duplicadas enquanto um `AudioContext` interrompido ainda está retomando. A regressão `audio-mobile-wake.test.js` cobre coalescência do resume e ausência de nós extras.

Isso reduz risco de wake duplicado, mas NÃO comprova restauração de áudio no Safari/iPhone. O pedido P1 `IR-CODEX-20260912-1518-LEAD-ios-mobile-audio.md` segue `READY` e exige reprodução real/estado do AudioContext. Não marcar o bug como resolvido até haver PASS real em mobile/Safari ou evidência equivalente.

### 3. Guerra persistente: fundações novas ainda isoladas
`persistent-war-clock.js` cria catch-up por wall clock e `theatre-sectors.js` cria seeds coerentes a partir de uma única linha territorial. Os testes recentes cobrem scheduler e coerência de fronts. Como essas peças ainda são fundações modulares e não substituem a inicialização atual observada em `game-v6.js`, não há regressão visível imediata detectada por inspeção.

**Risco de integração:** evitar duas autoridades simultâneas para tempo/território (`warSimulation` local + futura autoridade persistente). A futura ligação deve definir claramente quem é fonte de verdade antes de migrar `newState()`/`buildSector()`.

### 4. Saúde da build
O head revisado não possui status/check CI publicado. Várias entradas do `AGENT_LOG.md` continuam relatando impossibilidade de executar `npm test` completo por falha de resolução de `github.com` no runtime dos agentes. As regressões focais recentes são úteis, mas não substituem a suíte completa e o teste de navegador.

## Prioridade recomendada
1. P1: reproduzir/fechar áudio real em iPhone/Safari antes de qualquer novo balanceamento sonoro.
2. Integrar o lobby somente junto da fronteira de sessão/transport já planejada; não anunciar multiplayer entregue antes disso.
3. Antes de integrar guerra persistente, definir uma autoridade única para relógio e estado territorial para evitar simulação dupla/divergente.
4. Assim que houver runtime válido, executar `npm test` e `tests/v7-browser.mjs` contra o head integrado.

Nenhuma correção de gameplay foi aplicada neste ciclo: não foi encontrada regressão crítica isolada e corrigível com segurança sem concorrer com os workstreams ativos.
