# QA Governance — 2026-09-12 17:26 BRT

## Escopo revisado
- branch ativa `iron-rain-v6-1-continuation`; `iron-rain-frontline` preservada como histórica;
- `GITHUB_HANDOFF.md`, `ACTIVE_USER_DIRECTIVES_20260912.md`, `MULTI_AGENT_CONTROL.md`, `AGENT_LOG.md` e commits recentes;
- multiplayer P0, facções, exclusividade de postos, mobile AIM, PWA/service worker, áudio iPhone e estado dos testes.

## Achados
1. O contrato de sessão preserva máximo de 3 jogadores, Mamute por facção e rejeição de facção oposta. ALIADOS/EIXO continuam podendo existir no mesmo teatro sem compartilhar o mesmo veículo.
2. A autoridade de postos é exclusiva e libera ownership em leave/timeout. O runtime exige ownership antes de comandos físicos do Mamute.
3. `mobile-station-ui.css` segue a diretiva atual: handwheels duplicadas ficam escondidas no AIM touch; CARGA +/- e DISPARAR ficam compactos à direita; a área central permanece livre e as regras são restritas a `.input-touch`, sem regressão intencional de desktop.
4. BroadcastChannel continua restrito ao modo QA; produção agora usa `crew-mqtt-transport.js`. O transporte público é P0/field-test e não equivale a backend persistente/autenticado.
5. Regressão crítica de PWA encontrada: `bootstrap.js` importa `crew-cabin-bridge.js` e `crew-mqtt-transport.js`, mas ambos estavam ausentes de `OFFLINE_FILES` no `sw.js`. Isso poderia deixar o PWA instalado abrir online e depois falhar no bootstrap quando offline. Correção mínima aplicada: ambos foram adicionados ao cache e a versão do cache subiu de v7.8 para v7.9.
6. O mesmo URL/PWA continua sendo usado; o service worker permanece network-first para navegação/ativos conhecidos e limpa caches antigos pelo prefixo do mesmo scope.
7. Não há teste dedicado encontrado para `crew-mqtt-transport.js`; portanto cross-device real ainda precisa de validação de duas instâncias/dispositivos. O transporte usa brokers MQTT públicos anônimos, sem autenticação/E2E, adequado apenas como checkpoint P0 e não como autoridade de guerra persistente.
8. O bug de silêncio no iPhone/Safari continua aberto até teste real em aparelho. Nenhum tuning subjetivo foi feito nesta revisão.

## Correção realizada
- `iron-rain/sw.js`: cache v7.9 + inclusão de `./modules/crew-cabin-bridge.js` e `./modules/crew-mqtt-transport.js`.

## Próximos testes prioritários
1. duas instâncias/dispositivos no mesmo código e mesma facção: conectar, enxergar presença, respeitar máximo 3;
2. facção oposta: negar entrada no Mamute;
3. dois jogadores tentando o mesmo posto: apenas um dono, release em disconnect/timeout;
4. PWA: abrir online após deploy, fechar rede e reabrir pelo mesmo ícone/URL;
5. iPhone/Safari: confirmar áudio após gesto real e retorno de background;
6. mobile AIM em landscape: nenhuma handwheel UI duplicada, CARGA +/- + DISPARAR à direita, controles físicos 3D tocáveis;
7. desktop: mouse/teclado e fire deck não podem herdar os overrides touch.

## Risco residual
O maior risco imediato migrou do bootstrap offline para validação de transporte público real: a implementação cross-device acabou de entrar e ainda não tem evidência de teste end-to-end em dois dispositivos nesta revisão. Não anunciar multiplayer público como validado até esse checkpoint passar.
