# IRON RAIN — LEAD LIVE CHECKPOINT — 2026-09-12 18:20 BRT

Branch única: `iron-rain-v6-1-continuation`.

## Mudanças da liderança que já aterrissaram

- Mobile AIM: corrigida a causa raiz que impedia `mobile-station-ui.css` de aplicar. A regra antiga dependia de `.input-touch`, classe que o runtime atual não adiciona. Agora touch/coarse pointer usa diretamente `.inside.station-engaged` e esconde o deck 2D duplicado de azimute/elevação e munição, preservando somente `CARGA +/-` + `DISPARAR` compactos à direita; `AFASTAR-SE` continua separado; desktop não entra no media gate.
- Novo mapa estratégico player-facing integrado pelo `bootstrap.js`: botão `GUERRA` abre o teatro com grandes hexágonos e setores internos, ALIADOS azul / EIXO verde.
- O mapa consome `strategic-hex-map.js`, `territory-development.js`, `territory-ai.js` e `strategic-logistics.js`: mesma regra de desenvolvimento para ambos os lados, projetos exigem estoque realmente entregue, comboios debitam origem e só entregam ao chegar, e detalhes logísticos inimigos ficam ocultos sem intel.
- PWA mantém o MESMO URL e o cache foi elevado para `v7.11`, incluindo a nova integração estratégica.

## Instrução para todos os workstreams

Não recriar nenhuma dessas integrações e não reintroduzir UI mobile duplicada. Consumir este estado como novo baseline. Prioridade restante até o gate: validar build player-facing, integrar presença/ownership multiplayer, loader/impacto/manutenção e qualquer mapa/IA adicional somente se consumir o fluxo já vivo acima sem criar segundo estado concorrente.

Commits de referência da liderança: `69e026ab`, `c4a1bf49`, `1b2b6dbb`, `01cf66a6`, `dfc6020b`, `48e30d6c`.
