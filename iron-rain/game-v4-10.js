function drawPoints(){for(const p of state.points){const q=worldToScreen(p.x,p.y),col=p.owner==='ally'?COL.ally:p.owner==='enemy'?COL.enemy:'#ddd';ctx.strokeStyle=col;ctx.lineWidth=2;ctx.setLineDash([8,7]);ctx.beginPath();ctx.arc(q.x,q.y,p.r*cam.zoom,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle='rgba(5,10,7,.78)';ctx.fillRect(q.x-40,q.y-48,80,18);ctx.fillStyle=col;ctx.font='700 10px system-ui';ctx.textAlign='center';ctx.fillText(p.name,q.x,q.y-35);ctx.textAlign='start';for(const d of p.defenses)if(d.alive)drawDefense(d)}}
function drawDefense(d){const q=worldToScreen(d.x,d.y);ctx.save();ctx.translate(q.x,q.y);ctx.rotate(d.aim);ctx.fillStyle='#4d4a3f';ctx.fillRect(-10,-8,20,16);ctx.strokeStyle='#c9b979';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(d.type==='MORTAR'?15:22,0);ctx.stroke();if(d.muzzle>0){ctx.fillStyle='#ffd472';ctx.beginPath();ctx.arc(25,0,5,0,Math.PI*2);ctx.fill()}ctx.restore();ctx.fillStyle='rgba(0,0,0,.7)';ctx.fillRect(q.x-22,q.y-25,44,5);ctx.fillStyle=COL.enemy;ctx.fillRect(q.x-22,q.y-25,44*clamp(d.hp/d.maxHp,0,1),5);ctx.fillStyle='#fff';ctx.font='700 7px system-ui';ctx.textAlign='center';ctx.fillText(d.type,q.x,q.y-31);ctx.textAlign='start'}
function drawCovers(){for(const c of state.covers){const q=worldToScreen(c.x,c.y);ctx.fillStyle=c.type==='sand'?COL.sand:COL.wall;ctx.fillRect(q.x,q.y,c.w*cam.zoom,c.h*cam.zoom)}}
function drawCrates(){for(const c of state.craters){const q=worldToScreen(c.x,c.y);ctx.fillStyle='rgba(42,35,27,.65)';ctx.beginPath();ctx.ellipse(q.x,q.y,c.r*cam.zoom,c.r*.62*cam.zoom,0,0,Math.PI*2);ctx.fill()}for(const s of state.smokes){const q=worldToScreen(s.x,s.y);ctx.fillStyle='rgba(202,210,207,.22)';ctx.beginPath();ctx.arc(q.x,q.y,s.r*cam.zoom,0,Math.PI*2);ctx.fill()}}
function drawUnits(){for(const u of state.units){const q=worldToScreen(u.x,u.y);if(q.x<-20||q.x>screenW+20||q.y<-20||q.y>screenH+20)continue;ctx.save();ctx.translate(q.x,q.y);ctx.rotate(u.aim);ctx.fillStyle=u.team==='ally'?COL.ally:COL.enemy;ctx.beginPath();ctx.arc(-2,0,5,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#1a1e1b';ctx.lineWidth=2.5;ctx.beginPath();ctx.moveTo(2,0);ctx.lineTo(12,0);ctx.stroke();if(u.muzzle>0){ctx.fillStyle='#ffd875';ctx.beginPath();ctx.arc(14,0,3.5,0,Math.PI*2);ctx.fill()}ctx.restore();if(u.supp>.55){ctx.strokeStyle='#efd27a';ctx.lineWidth=1;ctx.beginPath();ctx.arc(q.x,q.y,8,0,Math.PI*2);ctx.stroke()}}}
