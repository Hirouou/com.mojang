function drawRobot(){const q=worldToScreen(state.robot.x,state.robot.y);ctx.save();ctx.translate(q.x,q.y);ctx.rotate(state.robot.facing);ctx.fillStyle='#9f8247';ctx.fillRect(-22,-15,44,30);ctx.fillStyle='#54472f';ctx.fillRect(-27,-18,8,36);ctx.fillRect(19,-18,8,36);ctx.restore();ctx.save();ctx.translate(q.x,q.y);ctx.rotate(state.bearing*Math.PI/180-Math.PI/2);ctx.fillStyle='#c5a65d';ctx.beginPath();ctx.arc(0,0,10,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#ead58a';ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(42,0);ctx.stroke();ctx.restore();ctx.fillStyle='rgba(5,8,6,.8)';ctx.fillRect(q.x-26,q.y-34,52,15);ctx.fillStyle='#f0d88a';ctx.font='700 9px system-ui';ctx.textAlign='center';ctx.fillText('M-47',q.x,q.y-23);ctx.textAlign='start'}
function drawShell(){if(!state.shell)return;const q=worldToScreen(state.shell.x,state.shell.y);ctx.fillStyle='#ffd66f';ctx.beginPath();ctx.arc(q.x,q.y,5,0,Math.PI*2);ctx.fill();ctx.fillStyle='#fff';ctx.font='9px system-ui';ctx.fillText('ALT '+Math.max(0,Math.round(state.shell.z))+'m',q.x+9,q.y-8)}
function drawFx(){for(const t of state.tracers){const a=worldToScreen(t.x,t.y),b=worldToScreen(t.x2,t.y2);ctx.strokeStyle=t.team==='ally'?'rgba(110,190,255,.78)':'rgba(255,120,105,.78)';ctx.lineWidth=t.heavy?2:1.3;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}for(const e of state.effects){const q=worldToScreen(e.x,e.y);if(e.type==='incoming'){ctx.strokeStyle='rgba(255,215,100,.75)';ctx.beginPath();ctx.arc(q.x,q.y,18,0,Math.PI*2);ctx.stroke()}else if(e.type==='blast'){const r=e.max*(1-clamp(e.life/.75,0,1))*cam.zoom;ctx.fillStyle=`rgba(255,153,68,${clamp(e.life/.75,0,1)*.45})`;ctx.beginPath();ctx.arc(q.x,q.y,r,0,Math.PI*2);ctx.fill()}}}
function updateUI(){const p=objective(),defs=activeDefs(p);UI.robot.textContent=`X${fmt(state.robot.x)} Y${fmt(state.robot.y)} • ${Math.max(0,Math.round(state.armor))}%`;UI.objective.textContent=p?`${p.name} • ARMAS ${defs.length}/${p.defenses.length}`:'OBJETIVO CONCLUÍDO';UI.phase.textContent=PHASES[state.operation.phase]||state.operation.phase;UI.wind.textContent=`VENTO ${state.wind.x>=0?'L':'O'} ${Math.abs(state.wind.x).toFixed(1)} / ${state.wind.y>=0?'S':'N'} ${Math.abs(state.wind.y).toFixed(1)}`;UI.front.textContent=`A:${alive('ally').length}+${state.reserveA} • E:${alive('enemy').length}+${state.reserveE}`;UI.speed.textContent=Math.round(state.robot.speed*0.6)+' km/h';UI.bearing.textContent=String(Math.round(state.bearing)).padStart(3,'0')+'°';UI.elev.textContent=Math.round(state.elevation)+'°';UI.charge.textContent=state.charge;UI.he.textContent=state.ammo.HE;UI.smoke.textContent=state.ammo.SMOKE;UI.frag.textContent=state.ammo.CLUSTER;if(state.latestRequest&&!UI.request.classList.contains('hidden'))UI.reqHint.textContent=state.operation.phase==='PREP'?'Ataque aliado aguardando seu fogo':'Toque para observar';}
